# Scripts

| File | Purpose |
|---|---|
| `inspect_checkpoint.py` | print the contents of a `models/*.pkl` checkpoint -- latent width, layer names, whether it holds the encoder alone or the full autoencoder |
| `export_tensorboard.py` | convert the event files in `logs/` into one tidy CSV of learning curves |
| `make_figures.py` |  the plotting code, regenerating Figures 6-9 |


